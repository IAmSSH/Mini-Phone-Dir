import React from 'react';
import { Link } from 'react-router-dom';

const Backbtn = (props) => {

    return (
        <Link to="/" className="btn btn-light mt-4 mb-4">BACK</Link>
    );
}

export default Backbtn;