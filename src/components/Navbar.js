import React from 'react';

const Navbar = (props) => {

    return (
        <nav className="navbar navbar-expand-lg navbar-dark bg-dark">
            <a className="navbar-brand text-white mx-auto">PHONE DIRECTORY</a>
        </nav>
    );
}

export default Navbar;


